import {fetchData, apiAuthGet} from '../../helpers/apiService';

export const listUserCars = () =>{
    return dispatch => {
        const fetchCars = async () =>{
            apiAuthGet("")
        }
    }
}